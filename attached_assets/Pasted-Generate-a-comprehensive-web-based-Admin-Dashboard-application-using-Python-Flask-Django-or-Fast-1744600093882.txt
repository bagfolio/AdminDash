Generate a comprehensive, web-based Admin Dashboard application using Python (Flask, Django, or FastAPI preferred) for the backend and a suitable frontend framework (like React, Vue, or standard HTML/CSS/JS) designed for complete management and control over fetching financial data from the Financial Modeling Prep (FMP) API and storing/manipulating it within a PostgreSQL database. Critically, this dashboard must provide direct, interactive access to the underlying PostgreSQL database content. First, provide the complete SQL DDL for the PostgreSQL schema, meticulously designed to store all fetched FMP data categories (company profiles, EOD prices, full financial statements ideally using JSONB for completeness, key metrics, ratios, analyst data, ownership, insider/political trades, ESG, news, dividends, peers, earnings history, estimates, DCF values, etc.) along with tables for API call logs, custom formula definitions, and potentially dashboard configuration. The Python backend must securely handle FMP API and database credentials (via environment variables), implement robust functions for calling all relevant FMP endpoints (assuming familiarity with FMP structures), perform thorough data cleaning/validation, and execute database CRUD (Create, Read, Update, Delete) operations via parameterized queries using psycopg2 or an ORM. The frontend dashboard UI must be highly customizable, sleek, modern, and provide these core management functionalities: 1) API & Data Monitoring: Display detailed logs of FMP API calls (timestamp, ticker, endpoint, status, errors) and provide a status overview of data freshness for tracked tickers within the database. 2) Fetching Control: Allow the admin to manually trigger specific FMP API calls for selected tickers or lists of tickers, define parameters (like date ranges/periods), queue these requests, and potentially configure/manage recurring data fetch schedules (e.g., enable/disable daily EOD updates). 3) Direct Database Management Interface: This is crucial – provide a powerful interface to directly browse, view, search, and potentially manually edit or delete records within the PostgreSQL database tables for specific tickers (e.g., view all stored metrics for AAPL, manually correct a faulty EOD price record, delete outdated news). All modifications made through this interface must result in permanent alterations to the database, providing full admin control over the stored data (implement appropriate safeguards/confirmations for destructive actions). 4) Custom Formula Engine: Include a sophisticated UI for defining custom metrics/formulas (referencing the Equations_Metrics.pdf concepts); allow selection of any stored data field from the database tables as input variables (e.g., SELECT netIncome FROM financial_statements WHERE ticker='AAPL' AND period='TTM'), define the formula using these variables, name the custom metric, and store the definition. The backend must then automatically calculate and permanently store the results of these custom formulas for all applicable stocks in a designated database table, updating them as underlying input data changes. Ensure comprehensive error handling, detailed logging throughout the backend and frontend, and assume the target AI understands FMP API data structures well enough to implement the fetching and processing logic based on these requirements.